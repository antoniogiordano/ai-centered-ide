# Phase decision closures

Date: 2026-07-31

| ID | Decision | Status |
| --- | --- | --- |
| Q1 | BrowserView + injected recorder (see `docs/notes/q1-browser-recorder.md`) | Closed |
| Q4 | Checkpoint = copy of touched files under IDE userData; exclude `.env*` | Closed |
| Q5 | SQLite (better-sqlite3) + versioned migrations + pre-migrate backup | Closed |
| Q6 | Allowlist read-only commands; fixed denylist (`rm -rf`, `git push`, `sudo`, …) | Closed |
| Q7 | Dark default, IBM Plex tokens, two-pane cockpit (v1) | Closed (v1) |
| Q10 | Internal CI builds via GitHub Actions; no code signing / auto-update | Closed for MVP |
